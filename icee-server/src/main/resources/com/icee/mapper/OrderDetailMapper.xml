<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.icee.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        INSERT INTO order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, amount) VALUES 
        <foreach collection="orderDetails" item="deatil" separator=",">
            (#{deatil.name}, #{deatil.image}, #{deatil.orderId}, #{deatil.dishId}, #{deatil.setmealId}, #{deatil.dishFlavor}, #{deatil.amount})
        </foreach>
    </insert>

    <select id="getTop10ByOrderIds" resultType="com.icee.entity.OrderDetail">
        select od.name,sum(od.number) as number from order_detail od where order_id in
        <foreach item="id" collection="orderIdList" separator="," open="(" close=")">
            #{id}
        </foreach>
        group by name
        order by number desc limit 10
    </select>
</mapper>
