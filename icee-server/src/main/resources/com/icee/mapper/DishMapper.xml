<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.icee.mapper.DishMapper">

    <update id="update">
        update dish
        <set>
            <if test="name!=null and name!=''">name=#{name},</if>
            <if test="categoryId!=null">category_id=#{categoryId},</if>
            <if test="price!=null"> price=#{price},</if>
            <if test="image!=null and image!=''">image=#{image},</if>
            <if test="description!=null and description!=''">description=#{description},</if>
            <if test="status!=null">status=#{status},</if>
            <if test="updateTime!=null">update_time=#{updateTime},</if>
            <if test="updateUser!=null">update_user=#{updateUser},</if>
        </set>
        where id=#{id}
    </update>

    <delete id="delete">
        delete from dish where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>



    <select id="page" resultType="com.icee.vo.DishVO">
        select d.* , c.name as categoryName from dish d left join category c on d.category_id=c.id
        <where>
            <if test="categoryId!=null">
                and d.category_id=#{categoryId}
            </if>
            <if test="name!=null and name!=''">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="status!=null">
                and d.status=#{status}
            </if>
        </where>
        order by d.update_time desc
    </select>

    <select id="getByIds" resultType="com.icee.entity.Dish">
        select * from dish where id in
        <foreach collection="dishIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <select id="getBySetmealId" resultType="com.icee.entity.Dish">
        select d.* from dish d where d.id in(select s.dish_id from setmeal_dish s where s.setmeal_id =#{setmealId})
    </select>

    <select id="getBySetmealIdAndStatus" resultType="com.icee.entity.Dish">
        select d.* from dish d where d.id in(select s.dish_id from setmeal_dish s where s.setmeal_id =#{setmealId}) and d.status=#{status}
    </select>

    <select id="getByIdsAndStatus" resultType="com.icee.entity.Dish">
        select * from dish where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        and status=#{status}
    </select>

</mapper>
